<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tong.mp1.mapper.usermapper">
    <select id="selectuserbyname" resultType="com.tong.mp1.domain.user" parameterType="String">
        select  * from t_user where name=#{name}
    </select>

    <select id="selectbyname" resultType="com.tong.mp1.domain.user">
        select  * from t_user where name=#{name}
    </select>
</mapper>